<Window x:Class="MonitoringApp.MonitorBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="硬件监控长条"
        Width="790" Height="58"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        ShowActivated="False" Focusable="False"
        WindowStartupLocation="Manual">
  <Window.Resources>
    <Style x:Key="MiniBtn" TargetType="Button">
      <Setter Property="Background" Value="#DCE6EF" />
      <Setter Property="Foreground" Value="#5A6B7E" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="FontSize" Value="10" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="5">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#C6D5E2" />
                <Setter Property="Foreground" Value="#24364A" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Border x:Name="Shell" CornerRadius="15" Background="#F2EFF5FB" BorderBrush="#C6D4E1" BorderThickness="1"
          Padding="10,4,6,4">
    <Border.Effect>
      <DropShadowEffect Color="#000000" BlurRadius="18" ShadowDepth="2" Opacity="0.32" />
    </Border.Effect>
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <!-- 六个指标块 -->
      <UniformGrid x:Name="Host" Columns="6" Grid.Column="0" MouseLeftButtonDown="OnBarDrag">
        <!-- 功耗 -->
        <Grid>
          <Grid.RowDefinitions><RowDefinition Height="20" /><RowDefinition Height="*" /></Grid.RowDefinitions>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Width="6" Height="10" CornerRadius="2" Background="#F97316" Margin="0,0,5,0" />
            <TextBlock Text="功耗" FontSize="11" Foreground="#67798D" VerticalAlignment="Center" />
          </StackPanel>
          <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="PVal" Text="--" FontSize="18" FontWeight="Bold" Foreground="#C2410C" VerticalAlignment="Center" />
            <TextBlock x:Name="PUnit" Text="W" FontSize="11" Foreground="#6C7E92" Margin="6,2,0,0" VerticalAlignment="Center" />
          </StackPanel>
        </Grid>
        <!-- 温度 -->
        <Grid>
          <Grid.RowDefinitions><RowDefinition Height="20" /><RowDefinition Height="*" /></Grid.RowDefinitions>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Width="6" Height="10" CornerRadius="2" Background="#34D399" Margin="0,0,5,0" />
            <TextBlock Text="温度" FontSize="11" Foreground="#67798D" VerticalAlignment="Center" />
          </StackPanel>
          <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="TVal" Text="--" FontSize="18" FontWeight="Bold" Foreground="#15803D" VerticalAlignment="Center" />
            <TextBlock x:Name="TUnit" Text="℃" FontSize="11" Foreground="#6C7E92" Margin="6,2,0,0" VerticalAlignment="Center" />
          </StackPanel>
        </Grid>
        <!-- 转速 -->
        <Grid>
          <Grid.RowDefinitions><RowDefinition Height="20" /><RowDefinition Height="*" /></Grid.RowDefinitions>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Width="6" Height="10" CornerRadius="2" Background="#2DD4BF" Margin="0,0,5,0" />
            <TextBlock Text="转速" FontSize="11" Foreground="#67798D" VerticalAlignment="Center" />
          </StackPanel>
          <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="FVal" Text="----" FontSize="18" FontWeight="Bold" Foreground="#0F766E" VerticalAlignment="Center" />
            <TextBlock x:Name="FUnit" Text="RPM" FontSize="11" Foreground="#6C7E92" Margin="6,2,0,0" VerticalAlignment="Center" />
          </StackPanel>
        </Grid>
        <!-- 内存 -->
        <Grid>
          <Grid.RowDefinitions><RowDefinition Height="20" /><RowDefinition Height="*" /></Grid.RowDefinitions>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Width="6" Height="10" CornerRadius="2" Background="#F472B6" Margin="0,0,5,0" />
            <TextBlock Text="内存" FontSize="11" Foreground="#67798D" VerticalAlignment="Center" />
          </StackPanel>
          <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="MVal" Text="--" FontSize="18" FontWeight="Bold" Foreground="#BE185D" VerticalAlignment="Center" />
            <TextBlock x:Name="MUnit" Text="%" FontSize="11" Foreground="#6C7E92" Margin="6,2,0,0" VerticalAlignment="Center" />
          </StackPanel>
        </Grid>
        <!-- 下行 -->
        <Grid>
          <Grid.RowDefinitions><RowDefinition Height="20" /><RowDefinition Height="*" /></Grid.RowDefinitions>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Width="6" Height="10" CornerRadius="2" Background="#60A5FA" Margin="0,0,5,0" />
            <TextBlock Text="下行" FontSize="11" Foreground="#67798D" VerticalAlignment="Center" />
          </StackPanel>
          <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="DVal" Text="--" FontSize="18" FontWeight="Bold" Foreground="#1D4ED8" VerticalAlignment="Center" />
            <TextBlock x:Name="DUnit" Text="KB/s" FontSize="11" Foreground="#6C7E92" Margin="6,2,0,0" VerticalAlignment="Center" />
          </StackPanel>
        </Grid>
        <!-- 上行 -->
        <Grid>
          <Grid.RowDefinitions><RowDefinition Height="20" /><RowDefinition Height="*" /></Grid.RowDefinitions>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Width="6" Height="10" CornerRadius="2" Background="#A78BFA" Margin="0,0,5,0" />
            <TextBlock Text="上行" FontSize="11" Foreground="#67798D" VerticalAlignment="Center" />
          </StackPanel>
          <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="UVal" Text="--" FontSize="18" FontWeight="Bold" Foreground="#6D28D9" VerticalAlignment="Center" />
            <TextBlock x:Name="UUnit" Text="KB/s" FontSize="11" Foreground="#6C7E92" Margin="6,2,0,0" VerticalAlignment="Center" />
          </StackPanel>
        </Grid>
      </UniformGrid>

      <Button Grid.Column="1" x:Name="HideBtn" Content="✕" Style="{StaticResource MiniBtn}" Width="26" Height="22"
              Margin="8,0,0,0" VerticalAlignment="Center" Click="OnHide"
              ToolTip="隐藏监控长条（单击托盘图标可重新唤出）" />
    </Grid>
  </Border>
</Window>
