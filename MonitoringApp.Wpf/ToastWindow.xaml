<Window x:Class="MonitoringApp.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="提示"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        ShowActivated="False" Focusable="False"
        SizeToContent="WidthAndHeight"
        Opacity="0">
  <Border x:Name="Root" Background="{StaticResource PanelBrush}" CornerRadius="14"
          BorderBrush="#E1E6ED" BorderThickness="1" Margin="16" Padding="18,14">
    <Border.Effect>
      <DropShadowEffect Color="#000000" BlurRadius="22" ShadowDepth="3" Opacity="0.5" />
    </Border.Effect>
    <StackPanel Orientation="Horizontal">
      <TextBlock x:Name="MarkText" Text="✓" FontSize="24" FontWeight="Bold" Foreground="{StaticResource GreenBrush}" VerticalAlignment="Top" Margin="0,1,12,0" />
      <StackPanel VerticalAlignment="Center">
        <TextBlock x:Name="TitleText" Text="" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
        <TextBlock x:Name="BodyText" Text="" FontSize="12.5" Foreground="{StaticResource TextDimBrush}" Margin="0,5,0,0" TextWrapping="Wrap" MaxWidth="340" />
      </StackPanel>
    </StackPanel>
  </Border>
</Window>
